# AI 引擎配置（v2.2：支持 GLM + DeepSeek 多引擎）
# eval 和 writer 共用同一引擎配置。
# default 指定启动时使用哪个引擎；可在 Web 端运行时切换。
default: bigmodel

bigmodel:
  provider: bigmodel
  baseUrl: https://open.bigmodel.cn/api/anthropic
  model: glm-5.3  # 2026-08-30 起默认；回退用 glm-5.3-flash（更快更省，Web 设置页下拉可切）
  maxBudgetRmb: 10
  perChapterMaxBudgetRmb: 0.1

deepseek:
  provider: deepseek
  baseUrl: https://api.deepseek.com/anthropic
  model: deepseek-v4-pro
  maxBudgetRmb: 10
  perChapterMaxBudgetRmb: 0.15
