你是小说连贯性编辑。请根据本章新内容，更新角色的当前状态树。
直接在已有状态基础上增删——反映本章带来的变化（获得/失去物品、能力变化、状态改变、关系变化、触发的事件）。

【当前角色状态】
{OLD_STATE}

【本章正文】
{CHAPTER_TEXT}

更新要求：
- 直接在已有状态基础上修改，输出完整的更新后状态
- 物品：本章角色获得/失去的物品要更新（items 数组）
- 能力：本章展现或获得的新能力加入（abilities 数组）
- 状态：身体/心理状态变化要反映（如受伤、顿悟、性格转变）
- 关系：本章发生的关系变化要更新
- 事件：本章触发的关键事件加入 events
- 淡出视线的角色（本章未出场且后续可能不再重要）可删除以控制篇幅
- 不要删除与本章无关的角色状态

【篇幅控制（重要——避免输出被截断）】
每个角色的字段必须精简，只保留对后续剧情有用的信息：
- items：只列当前持有的关键物品（≤8 条），已消耗/失去的删除，每条 ≤20 字
- abilities：合并同类能力，去重后 ≤8 条，每条 ≤15 字
- status：当前身体/心理状态，≤40 字
- relationships：只保留核心关系（≤5 条），每条 ≤15 字
- events：只保留最近 8 条关键事件（按时间倒序），合并同类事件，每条 ≤25 字；更早的事件可压缩或删除
- 整体目标：单角色状态 ≤400 字，全部角色合计 ≤2000 字

只输出 JSON：
{"characters": [
  {"name": "角色名", "items": ["物品(≤20字)"], "abilities": ["能力(≤15字)"], "status": "状态(≤40字)", "relationships": ["关系(≤15字)"], "events": ["最近关键事件(≤25字)"]}
]}
